@using Vidly.Models
@{
    ViewBag.Title = "List";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Movies</h2>

@if (Model.Count == 0)
{
    <div> Sorry, there are no movies</div>
}
else
{
    <table class=“table table-‐bordered table‐hover”>
        <thead>
        <tr>
            <th>Movie</th>
            <th>Genre</th>
        </tr>
        </thead>
        <tbody>

            @foreach (Movie movie in Model)
            {

                <tr><td>@Html.ActionLink((string)movie.Name, "Details", "Movies", new { id = movie.Id }, null)</td></tr>
                
                 <tr><td>@movie.Genre.Name</td></tr>

            }



</table>



            }
